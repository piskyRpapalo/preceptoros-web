# La huella de lo publicado, y la version de cache que le corresponde.
#
# POR QUE EXISTE. El 2026-09-13 se desplegaron `bronce.js`, `movimiento.css` y
# un `corregir.js` nuevo. Los tres llegaron al servidor --`curl` los traia con
# sus bytes exactos-- y la pagina seguia ejecutando los VIEJOS: `sw.js` servia
# el shell desde `shell-preceptoros-2026-11-d`, cacheado dias antes. La
# correccion se guardaba pero la puerta de exportacion no aparecia. Al Doogee
# de pruebas le pasaba lo mismo y se habia diagnosticado como «un boton sin
# rotulo»: no era CSS, era esto.
#
# O sea: cambiar un asset y no subir `VERSION` en `sw.js` significa que el
# cambio NO llega a quien ya visito el sitio. Nunca. Y no avisa nadie.
#
# COMO SE VIGILA. `huella` es el sha256 (16 hex) de todo lo que hay bajo
# `public/` menos `sw.js`. Si cambia un solo byte publicado y `version` sigue
# igual, `test_web.py` se pone rojo y dice que hay que subirla. Se comprueba,
# no se recuerda.
#
# VIVE AQUI Y NO DENTRO DE `sw.js` por dos razones: en ejecucion no la usa
# nadie --solo el gate-- y ese fichero esta a 40 B de su tope de 16 KiB y a 150
# de su tope de red. Meterle un dato que no necesita seria pagar el tope con
# algo que no hace falta que viaje.
#
# AL CAMBIAR UN ASSET: sube `version` en `public/sw.js` y pega aqui la huella
# nueva que el propio test te dice.
huella=d1e83e5074dd9856
version=preceptoros-2026-13-t
